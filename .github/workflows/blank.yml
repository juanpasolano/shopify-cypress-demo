# This is a basic workflow to help you get started with Actions

name: CI

# Controls when the action will run. Triggers the workflow on push or pull request
# events but only for the master branch
on:
  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  zip:
    runs-on: ubuntu-latest
    steps:
#       - uses: actions/checkout@v2
        
#       - uses: montudor/action-zip@v0.1.0
#         with:
#           args: zip -qq -r ./theme.zip ./theme

#       - uses: actions/upload-artifact@v2
#         with:
#           name: temptest.zip
#           path: ./temptest.zip

#       - uses: zdurham/s3-upload-github-action@master
#         with:
#          args: --acl public-read
#         env:
#           FILE: ./temptest.zip
#           AWS_REGION: 'us-east-1'
#           S3_BUCKET: ${{ secrets.AWS_S3_BUCKET }}
#           AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}

#       - run: echo -n -e "{\"theme\":{\"name\":\"GitHubActionsTest - $(date +"%D %T %Z")\",\"src\":\"$FILE_PATH\"}}" > body.json
#         env:
#           FILE_PATH: https://shopify-cypress-demo.s3.amazonaws.com/temptest.zip  
#       - run: echo $(cat body.json)

      - name: Get current date
        id: date
        run: echo "::set-output name=date::$(date +'%D %T %Z')"
      
      - uses: Satak/webrequest-action@v1.2.3
        id: webhook
        with:
          url: https://${{secrets.SHOPIFY_API_KEY}}:${{secrets.SHOPIFY_API_PASSWORD}}@${{secrets.SHOPIFY_URL}}/admin/api/2019-04/themes.json
          method: POST
          payload: '{"theme":{"name":"GitHubActionsTest - ${{steps.date.outputs.date}}","src":"${{env.FILE_PATH}}"}}'
        env:
          FILE_PATH: https://shopify-cypress-demo.s3.amazonaws.com/temptest.zip   
      - run: echo "${{ steps.webhook.outputs.output }}"
          
      - id: data
        uses: gr2m/get-json-paths-action@v1.x
        with:
          json: ${{ steps.webhook.outputs.output }}
          theme_id: "data.theme.id"
      - run: echo "${{ steps.data.outputs.theme_id }}"
